# react-au

Simple library to login into auth service (dummy).

## Installation

Download and install library:
```react-native
npm i react-au
```

## Import
Import in your code
```react-native
import {login} "react-au"
```